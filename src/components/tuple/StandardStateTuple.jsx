import { StandardDataTuple } from "./StandardDataTuple.jsx";
import styled from "styled-components";

const StandardStateTuple = styled(StandardDataTuple)`
  .value {
    font-size: var(--tx-sm);
    color: var(--info-base);
  }
`;

export { StandardStateTuple };
